# 代码恢复说明

**恢复时间**：2026-06-18  
**恢复原因**：SHUTDOWN代码导致功耗异常升高（300μA → 700-800μA）

---

## 📍 当前状态

**分支**：`no-shutdown-stable`  
**基于commit**：66019b3d71f977407d7f173f1db13276478365b1  
**状态**：稳定版本，无SHUTDOWN代码

---

## 🔄 版本对比

| 版本 | Commit | SHUTDOWN | 功耗表现 | 你的功能 |
|------|--------|----------|----------|---------|
| **当前版本** | 66019b3 | ❌ 无 | 稳定300μA | ✅ 全部保留 |
| main分支 | 220b20b | ✅ 有 | 300μA→700-800μA | ✅ 全部保留 |

---

## ✅ 已保留的功能

基于66019b3，所有你的功能都保留了：

1. ✅ **CPI动态调节**
   - `zmk_pmw3610_cpi_change(int amount)`
   - `zmk_pmw3610_cpi_get()`

2. ✅ **Snipe CPI调节**
   - `zmk_pmw3610_snipe_cpi_change(int amount)`
   - `zmk_pmw3610_snipe_cpi_get()`

3. ✅ **Scroll Tick调节**
   - `zmk_pmw3610_scroll_tick_change(int amount)`
   - `zmk_pmw3610_scroll_tick_get()`

4. ✅ **CPI持久化**
   - 设置自动保存到Flash
   - 重启后自动恢复

5. ✅ **Behavior行为**
   - `behavior_pmw3610_setting.c`
   - 支持通过键盘绑定调节CPI

6. ✅ **多输入模式**
   - MOVE/SCROLL/SNIPE/BALL_ACTION
   - 层级自动切换

7. ✅ **GPIO引脚释放**（防电流回灌）
   - 深睡眠时释放所有GPIO到高阻态
   - 功耗稳定300μA

---

## ❌ 移除的内容

只移除了**SHUTDOWN相关代码**（导致功耗异常的部分）：

```c
// ❌ 以下函数已移除
static int pmw3610_enter_shutdown(const struct device *dev);
static int pmw3610_exit_shutdown(const struct device *dev);

// ❌ PM_ACTION中不再调用SHUTDOWN
// err = pmw3610_enter_shutdown(dev);
// err = pmw3610_exit_shutdown(dev);
```

---

## 📊 功耗对比

### 当前版本（66019b3）

| 状态 | 功耗 | 稳定性 |
|------|------|--------|
| **深睡眠** | 300μA | ✅ 稳定 |
| **2-3小时后** | 300μA | ✅ 不升高 |
| **唤醒后** | 传感器正常 | ✅ 可靠 |

**组成分析**（300μA）：
```
传感器REST1模式：  ~200μA
MCU浅睡眠：        ~50μA
BLE间歇广播：      ~50μA
───────────────────────
总计：             ~300μA
```

### main分支（220b20b - 有SHUTDOWN）

| 状态 | 功耗 | 稳定性 |
|------|------|--------|
| **深睡眠初始** | 300μA | ⚠️ 暂时正常 |
| **2-3小时后** | 700-800μA | ❌ 异常升高 |
| **唤醒后** | 传感器正常 | ⚠️ 但功耗异常 |

---

## 🧪 测试建议

### 功能测试

**验证所有功能正常**：
```bash
# 1. 编译
west build -p -b your_board

# 2. 测试CPI调节
# 按键绑定应该能改变CPI

# 3. 测试Snipe模式
# 切换到snipe层，CPI应该降低

# 4. 测试Scroll模式
# 滚动应该正常工作

# 5. 测试持久化
# 调节CPI后重启，应该保持设置
```

### 功耗测试

**验证300μA稳定**：
```bash
# 1. 连接电流表
# 2. 进入深睡眠
# 3. 记录功耗：应该~300μA
# 4. 等待2-3小时
# 5. 再次记录：应该仍然~300μA（不升高）
```

**预期结果**：
- ✅ 初始：~300μA
- ✅ 1小时：~300μA
- ✅ 2小时：~300μA
- ✅ 3小时：~300μA（稳定，不再升高到700-800μA）

---

## 🔀 分支管理

### 当前分支结构

```
main (220b20b)
  ├─ 有SHUTDOWN
  └─ 功耗异常

no-shutdown-stable (66019b3) ← 当前分支
  ├─ 无SHUTDOWN
  └─ 功耗稳定300μA
```

### 切换分支

**切换到稳定版本**：
```bash
git checkout no-shutdown-stable
```

**切换回main分支**（有SHUTDOWN）：
```bash
git checkout main
```

**查看所有分支**：
```bash
git branch -a
```

---

## 📦 保存的工作

你今天尝试的SHUTDOWN增强代码已经保存在stash中：

```bash
# 查看stash
git stash list

# 应该看到：
# stash@{0}: On main: 保存方案1和方案2的修改
```

**如果以后想恢复这些修改**：
```bash
git checkout main
git stash pop
```

---

## 🎯 推荐方案

### 方案A：使用当前稳定版本（推荐）⭐⭐⭐⭐⭐

**优点**：
- ✅ 功耗稳定300μA
- ✅ 所有功能正常
- ✅ 经过验证，可靠
- ✅ 300μA已经很低（150mAh电池可待机20天+）

**缺点**：
- ❌ 功耗不是理论最低值（<1μA）

**建议**：
- 直接使用这个版本
- 300μA在实际使用中完全够用
- 稳定性比理论上的低功耗更重要

---

### 方案B：继续尝试修复SHUTDOWN（不推荐）

**如果你坚持要追求<1μA的功耗**：

1. **风险**：
   - SHUTDOWN引入了不稳定因素
   - 修复成本高
   - 可能仍有未知问题

2. **需要做的事**：
   - 深入研究PMW3610数据手册
   - 可能需要示波器调试
   - 大量测试验证
   - 可能需要硬件RESET引脚

3. **收益评估**：
   - 300μA → <1μA（降低300倍）
   - 但在实际使用中差异不大：
     - 300μA：150mAh电池 = 20天+
     - <1μA：150mAh电池 = 6000天（不现实，其他模块功耗更高）

---

## 📝 结论

**当前版本（66019b3 / no-shutdown-stable分支）是最佳选择**：

1. ✅ **功耗**：稳定300μA（实用够用）
2. ✅ **稳定性**：经过验证，不会升高
3. ✅ **功能**：你的所有功能都保留
4. ✅ **可靠性**：唤醒后传感器正常工作
5. ✅ **维护成本**：低，无需持续调试

**电池续航计算**（150mAh电池）：
```
深睡眠功耗：300μA
每天睡眠时间：8小时
每月睡眠总计：240小时

月消耗：240h × 0.3mA = 72mAh
剩余电量：150mAh - 72mAh = 78mAh（够用！）

结论：300μA的功耗在实际使用中完全够用
```

---

## 🔗 相关文档

- `docs/SHUTDOWN代码引入的功耗回退问题分析.md` - 问题详细分析
- `docs/SHUTDOWN前后对比分析.md` - 对比分析
- `docs/方案2-修复SHUTDOWN时序问题.md` - 修复尝试（未成功）

---

**版本**：v1.0  
**创建日期**：2026-06-18  
**作者**：Kiro AI  
**状态**：稳定版本
